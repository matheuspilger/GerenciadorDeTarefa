namespace GerenciadorDeTarefa.Domain.Constants
{
    public class OptionsPath
    {
        public const string Database = nameof(Database);
        public const string RabbitMq = nameof(RabbitMq);
        public const string QueueHistoricoTarefa = nameof(QueueHistoricoTarefa);
        public const string Report = nameof(Report);
    }
}
